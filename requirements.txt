# AI Gateway
anthropic>=0.116
google-genai>=1.75           # Gemini (google-generativeai는 EOL — google-genai로 대체)
openai>=2.44                 # DeepSeek (OpenAI 호환)

# 설정·비동기·DB
pydantic-settings>=2.14
aiohttp>=3.9,<3.10           # aioresponses(0.7.x)가 3.10+의 ClientResponse 시그니처를 아직 지원하지 않음
asyncpg>=0.31
redis>=8.0                   # asyncio 지원이 기본 내장되어 [asyncio] extra 불필요
SQLAlchemy[asyncio]>=2.0.51

# 스케줄러·로깅
APScheduler>=3.11
structlog>=26.1

# 시장 데이터·분석
defusedxml>=0.7             # 외부 RSS XML 파싱 — 엔티티 확장·XXE 차단 (core/market_data/news.py)
pandas>=3.0
ta>=0.11                     # RSI·MACD·EMA·볼린저밴드
matplotlib>=3.11             # 리포트 그래프

# 헬스 모니터링
psutil>=7.2

# Discord 알림 (헬스 모니터링용)
discord.py>=2.7

# 환경변수
python-dotenv>=1.2

# 테스트·품질
pytest>=9.1
pytest-asyncio>=1.4
aioresponses>=0.7.9        # toss/client.py 등 aiohttp 호출 목업
fakeredis>=2.36            # Redis 캐시·Rate Limit 로직 인메모리 테스트
mypy>=2.1
types-defusedxml>=0.7      # mypy용 defusedxml 스텁
ruff>=0.15
